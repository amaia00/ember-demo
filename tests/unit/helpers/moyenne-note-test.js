
import { moyenneNote } from 'demonstration/helpers/moyenne-note';
import { module, test } from 'qunit';

module('Unit | Helper | moyenne note');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moyenneNote([42]);
  assert.ok(result);
});

